/**
 * Compatibility re-export — maintains the old import path.
 * New code should import from `@/components/workspace/environment-selector`.
 */
export { EnvironmentSelector } from "@/components/workspace/environment-selector";
